<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="ItemImage">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@drawable/bubble</item>
    </style>

    <style name="ItemCornerMark">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">top|end</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize" tools:ignore="SpUsage">10dp</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:typeface">monospace</item>
        <item name="android:background">@drawable/shape_corner_mark</item>
        <item name="android:drawablePadding">4dp</item>
        <item name="android:drawableLeft">@drawable/shape_corner_mark</item>
        <item name="android:drawableRight">@drawable/shape_corner_mark</item>
    </style>

</resources>
